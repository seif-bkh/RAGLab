# Minimal, deliberate dependency list. No orchestration frameworks (no LangChain, no LlamaIndex).
chromadb>=1.0,<2
python-dotenv>=1.0
pypdf>=4.0
tiktoken>=0.7

# Official Google client — default embedding provider for AI Studio keys
# (EMBEDDING_PROVIDER = "gemini", model `gemini-embedding-2` in config.py).
google-genai>=1.0,<2

# --- Optional providers -----------------------------------------------
# Uncomment exactly one line when you switch EMBEDDING_PROVIDER in config.py.
# For EMBEDDING_PROVIDER = "openai"  -> model e.g. text-embedding-3-large:
# openai>=1.30
# For EMBEDDING_PROVIDER = "cohere"  -> model e.g. `embed-multilingual-v3.0`:
# cohere>=5.10
# For EMBEDDING_PROVIDER = "voyage"  -> model e.g. `voyage-multilingual-2`:
# voyageai>=0.2

# For EMBEDDING_PROVIDER = "huggingface" (LOCAL, free, no API key, no quota).
# Recommended model (default): Qwen/Qwen3-Embedding-0.6B (Apache-2.0, 1024 dims,
# 32K context, 100+ languages incl. Arabic). Alternative: BAAI/bge-m3 (MIT).
# Installs torch — CPU wheels are used automatically on machines without CUDA.
# sentence-transformers>=3.2

