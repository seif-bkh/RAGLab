# خارطة طريق الانتقال إلى الوضع المستهدف — خطة التنفيذ

**المرجعية:** هذه الوثيقة هي عقد التنفيذ الملزم للانتقال من الوضع الراهن (موثقًا في
`RAGLAB_SPEC.md`) إلى الوضع المستهدف (تقرير الهدف الخارجي، 2026‑09‑24). تشخيص الفجوات
ومسنداتها في `RAGLAB_GAP_ANALYSIS.md`؛ هذه الوثيقة تجيب عن سؤال التنفيذ: ماذا، بأي
ترتيب، وبأي بوابة إغلاق.

**القرارات المعتمدة من صاحب القرار (2026‑09‑24):**

1. **نطاق المدونة التجريبية:** حذف بنك أطلس الافتراضي كليًا والعمل على **بنك البركة
   تونس** — تم التنفيذ: ملفا `raglab/data/fiche_produit_banque_albaraka_{ar,fr}.md`
   مبنيان من المصادر العمومية الرسمية للبنك (الموقع albaraka.com.tn + وثيقة الشروط
   التعريفية 2023)، بتاريخ جمع معلن وقسم مصادر صريح. ليست وثائق رسمية صادرة عن البنك.
2. **قرار تفعيل البحث الهجين (rrf) كافتراضي:** يُحسم **بالأرقام من خط الأساس**
   (الذراع الحية في CI)، لا مسبقًا.
3. **لغة هذه الوثيقة:** العربية، مثل وثيقة الفجوات.

**المبدأ الحاكم للتنفيذ:** كل مرحلة تُغلق بقياس على مجموعة الاختبار وبالتزام أخضر في
CI؛ لا مرحلة تنتقل للتي بعدها دون اجتياز بوابة إغلاقها. وكل تغيير في الخدمة **إضافي
فقط** (تجميد النهايات، AGENTS.md §2.7)، وكل قياس حي عبر `real-test.yml` حصرًا (شبكة
بيئة التطوير محجوبة عن مزودي النماذج)، والبوابة المحلية
`./raglab/run_tests.sh --offline` خضراء قبل كل دفعة.

---

## حالة المراحل

| المرحلة | الحالة |
|---|---|
| 1. التوثيق والمقابلة | **منجزة** — `RAGLAB_SPEC.md` + `RAGLAB_GAP_ANALYSIS.md` (CI أخضر عليهما) |
| 2. مجموعة الاختبار وخط الأساس | **قيد التنفيذ — البنية أنجزت، بقي الذراع الحية** (التفصيل أدناه) |
| 3. التدخلات الرخيصة عالية العائد | لم تبدأ |
| 4. طبقة المعرفة على نطاق ضيق (البركة) | لم تبدأ |
| 5. الفهم وخطة الدليل والكفاية | لم تبدأ |
| 6. إتمام عقد المخرجات والتحقق والتدقيق | لم تبدأ |
| 7. الصلاحيات وإدارة التغيير والتوسع | لم تبدأ |

---

## المرحلة 2 — مجموعة الاختبار وخط الأساس (قيد التنفيذ)

### ما أنجز اليوم (2026‑09‑24)

1. **المدونة:** حذف ملفي أطلس وخرائط مقاطعهما؛ ملفا البركة (ar/fr) بديلًا عنهما؛
   خرائط مقاطع مُصاغة لهما ومُتحقق منها (`chunk_maps.py check`: 12+14 مقطعًا، صفر
   ملاحظات بنيوية — **بانتظار مراجعة بشرية لحدود كل مقطع**).
2. **الفئات:** `evaluate.py` يقبل الآن تسع فئات: الأربع الأصلية حرفيًا +
   `colloquial / synonyms / implicit / compound / ambiguous` (التجميع بالفئة ديناميكي
   أصلًا فلم يلزم تعديل غير بوابة التحميل).
3. **مخطط الدليل المتعدد:** حقل `expected_substrings` (قائمة أدلة) لحالات الضمني
   والمركب، بدلالة hit@k محسومة عبر **رتبة اكتمال المتطلبات**
   (`requirement_completion_rank`): hit@k = كل الأدلة المطلوبة مغطاة ضمن أول k نتيجة.
4. **مقاييس §9 الجديدة:** «الدليل المتوقع ضمن السياق النهائي» (`evidence_in_context` —
   محاكاة `answer.build_sources` بميزانية 3000 رمز) و«تغطية المتطلبات»
   (`requirements.mean_coverage_top_k / all_requirements_top_k /
   all_requirements_in_context`) — كلها مفاتيح إضافية لا تكسر المتصلات القائمة.
5. **`questions_v2.json`** (38 حالة): 7 عامي تونسي، 7 مرادفات مؤسسية، 7 ضمني
   (متعددة الأدلة)، 7 مركب، 5 غامض (مع `ambiguity_note` يوثق الغموض المؤثر وما يجب
   استيضاحه — وقود المرحلة 5)، 5 خارج النطاق. جميع الأدلة المتوقعة متحقق منها ضد
   المدونة (صفر مشاكل).
6. **هجرة `questions_50.json`:** الحالات العشرون الخاصة بأطلس أُعيدت كتابتها على
   البركة بنفس المعرفات والفئات واللغات (17 ar / 17 fr / 16 en كما كانت)؛ والمجموعة
   القديمة `questions.json` أُعيد توجيه حالاتها الثلاث عشرة المتعلقة.
7. **أداة الاستقلال عن النموذج `answer_ab.py`:** استرجاع واحد لكل سؤال يُغذّى لكل
   النماذج المطلوبة؛ قياس: اتفاق الحالة، Jaccard لمجموعات المصادر، معدلات الامتناع
   ورفض البوابة لكل نموذج، وقائمة الأسئلة المتفاوتة مضمونًا («الاختلاف في الصياغة لا
   المضمون» يصبح رقمًا). مغطاة باختبارات دون اتصال بمولّدات مزيفة.
8. **خط الأساس المحلي (BM25، حتمي، دون اتصال):**

   **questions_50 بعد الهجرة** (45 قابلة للإجابة، k=20؛ بعد إصلاح بنية التعرفة الفرنسية):
   | الذراع | hit@1 | hit@3 | hit@5 |
   |---|---|---|---|
   | size‑220/40 | 42% | 62% | 69% |
   | restructure‑220/40 | **49%** | **80%** | **82%** |

   **questions_v2 الجديدة** (33 قابلة للإجابة، k=20):
   | الفئة | size hit@1 | restructure hit@1 |
   |---|---|---|
   | عامي (7) | 0% | **57%** |
   | مرادفات (7) | 29% | **57%** |
   | ضمني (7) | 14% | **43%** (86% عند k=3) |
   | مركب (7) | 43% | **57%** (86% عند k=3) |
   | غامض (5) | 40% | 40% |
   | **الإجمالي** | 24% | **52%** |

   قراءة أولية: أعراض تقرير الهدف الأصلية قابلة للقياس فعلًا — فئة العامي عند
   المعجمي البحت شبه معدومة (0%) وهي الفجوة التي يعالجها المعجم المؤسسي (المرحلة 3)،
   والضمني/المركب يكشفان الحاجة للتوسع العلاقي (المرحلة 5). خارج النطاق: أعلى درجة
   top‑1 = 23.3 (size) / 26.4 (restructure).

9. **التوثيق:** إدراج `questions_v2.json` في قوائم مجموعات الخدمة والواجهتين
   (إضافة اختيارية) + ترقيم `SERVICE_VERSION` إلى 1.3.0 + تحديث CONTRACT/SERVICE.
10. **مراجعة تقنية ذاتية للمدونة والخرائط (قبل المراجعة البشرية، 2026‑09‑24):**
    عُالجت ثلاث ملاحظات: (أ) إدراج العناوين الفرعية 4.1–4.5 للتعرفة في الملف الفرنسي
    لاستعادة التناظر البنيوي مع العربي (كانت غائبة دون قصد — تحسّن خط أساس q50:
    restructure hit@1 من 47% إلى 49%)؛ (ب) تقسيم جداول التعرفة في خرائط المقاطع
    اليدوية إلى مقطع لكل جدول (كانت الخمسة محشورة في مقطع واحد 331–402 رمز)؛
    (ج) تثبيت التبليط الكامل للخرائط على النص المطبَّع بإحداثياته الصحيحة — حارس
    بصمة الخريطة (`semantic_chunking.load_map`) هو من كشف خطأ الإحداثيات قبل وصوله.
    الخرائط الآن: عربية 16 مقطعًا / فرنسية 18، `chunk_maps.py check` سليم كله.

### المتبقي لإغلاق المرحلة 2

- [ ] **الذراع الحية في CI:** تشغيل `real-test.yml` على المجموعتين الجديدتين
      (vector وrrf) — يحدد قرار تفعيل الهجين بالأرقام (القرار المعتمد أعلاه).
- [ ] **توصيل `answer_ab.py` بـ CI:** خطوة اختيارية في `real-test.yml` تقارن نموذجي
      إجابة (xKiro المثبت + NVIDIA/chat أو احتياط Google) على `questions_v2.json`.
- [ ] **مراجعة بشرية** لخرائط مقاطع ملفي البركة (حدود المقاطع المصاغة آليًا).
- [ ] تثبيت أرقام الذراع الحية هنا وفي AGENTS.md §7 عند أول تشغيل أخضر.

**بوابة الإغلاق:** أرقام خط الأساس للفئات الجديدة على الذراعين (المحلية منجزة، الحية
متبقية) + المجموعتان تعملان دون اتصال في CI + أداة الاستقلال تعمل في CI.

---

## المرحلة 3 — التدخلات الرخيصة عالية العائد

| # | التدخل | التنفيذ | القياس |
|---|---|---|---|
| 1 | تفعيل الهجين كافتراضي | **بالبيانات فقط**: إذا تفوق rrf على vector في خط الأساس الحي (المجموعتان) دون تراجع في verbatim/OOS → تغيير افتراضي `profiles.default_state` (وضع الخدمة) + توثيق؛ وإلا يبقى vector ويُوثق السبب | Δhit@k لكل فئة، ذراع حية |
| 2 | المعجم المؤسسي المحكوم | وحدة `lexicon.py`: جدول مُدار (عامي→رسمي، اختصارات، تجاري، مقابلات ar/fr/en) كتوسيع استعلام **حتمي** قبل التضمين وBM25 + إلحاقه بسطح البحث المعجمي؛ مفصول صراحة عن مسار الترجمة المتقاعد (لا يفعّل `QUERY_TRANSLATION_ENABLED` أبدًا) | فئتا العامي والمرادفات في v2 |
| 3 | Reranker حتمي بالميزات | ترتيب بعد الدمج بإشارات: تطابق المصطلحات، العنوان، سطر السياق، عقوبات boilerplate — دون اتصال؛ API reranker خلف env لتجارب CI اختياريًا | hit@1 مقابل hit@3/5 (سد فجوة الفقد بالترتيب) |
| 4 | مرشحات الإصدار/الصلاحية | **مؤجلة عمدًا** إلى ما بعد المرحلة 4 (لا مرشح قبل بياناته) | — |

**بوابة الإغلاق:** تحسّن دالّ في عامي/مرادفات/ضمني بلا أي تراجع في verbatim/OOS،
مثبتًا على الذراع الحية لا المحلية فقط. أي رقم يُنشر لا يُنسب للمسار المثبت
(سياسة الانبوب).

## المرحلة 4 — طبقة المعرفة على نطاق ضيق (ملفا بنك البركة)

1. **`units.py`** — الوحدات المعرفية بأنواع وظيفية بالمعيار النفعي (نوع يُقبل فقط إذا
   غيّر سلوكًا): تعريف، قاعدة أهلية، شرط، استثناء، مبلغ/حد/رسم، مستند مطلوب، خطوة
   إجرائية، حكم زمني، نص عام. معرّف مستقر عبر الإصدارات، نص أصلي حرفي، مسار عناوين،
   سطر وصفي يُفهرس كسطح استرجاع إضافي (لا تلخيصًا توليديًا).
2. **السجل المركزي:** توسيع `docstore` بمحاور حوكمة دنيا (نوع الوثيقة، سلطة المرجعية،
   إصدار، تاريخ سريان، جمهور، نطاق) — قرار موصى به قابل للنقض.
3. **العلاقات الدنيا:** إرساء (وحدة→منتج/رسم/شرط) + خطابية (استثناء من، يقيّد، يعدّل،
   رأس جدول لـ) في جداول علائقية — لا مخزن رسوم.
4. **المسار المهيكل للأرقام:** جدول التعرفة (البطاقات/الحسابات/العروض/الخدمة عن بعد)
   يُستخرج إلى بيانات مهيكلة تُستعلم برمجيًا — نقطة نهاية جديدة (إضافية).
5. فهرسة الوحدات في مجموعة تجريبية عبر آلة الهجين القائمة.

**بوابة الإغلاق:** أسئلة البركة في v2 على مجموعة الوحدات ≥ خط الأساس الحالي في ظهور
الدليل الصحيح، وفحوص التغطية/البصمات خضراء.

## المرحلة 5 — الفهم وخطة الدليل والكفاية (من الصفر — أوسع بناء)

1. **`intent.py` حتمي-أولًا:** أنماط معجمية محكومة + كتالوج المنتجات من مخرجات
   المرحلة 4؛ نوع الطلب، النطاق، عام/شخصي (تعميم الحارس القائم)، أسئلة فرعية للمركب
   فقط، كشف الغموض المؤثر (استهلاك `ambiguity_note` المصرّحة في v2). **المسار
   الافتراضي عند تعذر التصنيف: استرجاع هجين واسع بلا افتراضات** — التصنيف للتوجيه
   لا للقبول/الرفض؛ نسبة غير المصنف مقاسة كل مرحلة.
2. **`evidence_plan.py`:** مطالب مشتقة من نوع النية دون افتراض النتيجة (رسم →
   المنتج الحاكم + الإعفاءات + الإصدار الساري + القناة/العملة؛ أهلية → الشروط +
   الاستثناءات + النطاق الزمني).
3. **التوسع العلاقي بسياسة النية:** حواف مسموحة، عمق ≤ 2، حد عقد؛ **الاستثناء يُسترجع
   دائمًا مع قاعدته** (قاعدة صلبة).
4. **`sufficiency.py`:** فحوص حتمية (سريان، انطباق، اكتمال تقييد، إصدار أحدث) + قياس
   تغطية المطالب (على `expected_substrings` نموذجًا) + كشف التعارض بأسبقية قابلة
   للضبط + إصعاد عند تعذر الحسم + جولات بحث موجهة بحد أقصى (جولات/زمن/سياق).
5. **حالة الكفاية تصل إلى التوليد** (كافٍ/جزئي/غامض/غير كافٍ/مقيد) — حقول استجابة
   اختيارية جديدة فقط (تجميد النهايات).

**بوابة الإغلاق:** تحسّن الضمني/المركب/الغامض على خط الأساس؛ الامتناع مرتبط بغياب
الدليل أو حدود الصلاحية لا بفشل التصنيف.

## المرحلة 6 — إتمام عقد المخرجات والتحقق والتدقيق (نصفها قائم)

1. استشهاد بمعرّف وحدة مستقر + توسيع البوابة حتميًا (الاستشهاد لوحدة مسترجَعة فعلًا
   ومسموحة ونافذة).
2. التزام الإجابة بحالة الكفاية المعطاة + إعادة توليد مقيدة عند فشل التحقق + تخفيض
   إلى جزئي + مسار إحالة.
3. **`audit.py`:** سجل تدقيق لكل طلب بمعرّف تتبع (النية، الخطة، المسترجَع وترتيبه،
   المرشحات، الإصدارات، الكفاية، المراجع) + سياسة احتفاظ + حجب PII.
4. تحديث CONTRACT/COOKBOOK + ترقيم الإصدار مع كل إضافة.

**بوابة الإغلاق:** معايير §9 القابلة للقياس خضراء؛ إعادة تشغيل السؤال نفسه تعطي نفس
الأثر.

## المرحلة 7 — الصلاحيات وإدارة التغيير والتوسع

1. مستخدمون عبر رؤوس موثوقة من البوابة (`X-User-Id/Roles` خلف service token) +
   ترشيح إلزامي سابق للبحث على جمهور الوثائق/الوحدات + فصل استخدام/إفصاح.
2. دورة إدارة التغيير: نشر إصدار وثيقة (سجل → استخراج → وحدات → علاقات → فهرسة →
   نشر إصدار متسق بعد الاختبارات + علاقات إحلال).
3. توسيع النطاق تدريجيًا من المنتج التجريبي إلى المدونة بمعايير البيانات نفسها.

**قرارات خارجة عن الشيفرة تنتظر صاحب القرار:** مزوّد الهوية والأدوار، سياسة الإفصاح
عن الوجود، أسبقية المرجعيات المعتمدة لدى البنك.

---

## المخاطر وضوابطها (تسري على كل المراحل)

| الخطر | الضابط |
|---|---|
| الذراع المحلية تقيس BM25 فقط | كل قرار هجين/reranker يُثبَّت على الذراع الحية في CI |
| لا بيانات إصدار/جمهور بعد | لا مرشح قبل بياناته (المرحلة 4 تسبق المرشحات) |
| التصنيف الحتمي محدود النطاق | نسبة غير المصنف مقاسة + مسار افتراضي واسع يمنع الرفض بفشل التصنيف |
| تجميد النهايات يقيد الحالات الجديدة | حقول اختيارية أو نقاط جديدة فقط؛ لا تعديل دلالات `/answer` |
| البيئة المحلية لا تبقى (`.venv` خارج اللقطات) | إعادة الإنشاء أمر واحد موثق في AGENTS.md §3 |
| ضخامة البناء | كل مرحلة تسليم مستقل قابل للتوقف؛ لا اعتماد دائري |

## قواعد إسناس الأرقام (دائمة)

- الأرقام المنشورة قبل 2026‑09‑24 تعود للمجموعة القديمة (أطلس) وتظل سجلاً تاريخيًا؛
  المقارنات الجديدة تجري على مجموعة البركة (`questions_50` المهاجرة + `questions_v2`).
- لا رقم يُنسب للمسار المثبت (زوج NVIDIA/xKiro) إلا إذا قيس به؛ طبقات جديدة =
  مسارات قياس مستقلة معلنة.
- الفرق الأصغر من سؤال واحد بين تشغيلتين حيتينين = ضوضاء (jitter موثق في AGENTS §7).
